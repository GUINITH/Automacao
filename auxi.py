# Pasta auxiliar para pegar os campos a serem clicados no mouse
import pyautogui as pa 
import time 

time.sleep(5) # intervalo para printar a posição
print(pa.position()) # Printar a posição do mouse, para pegar o campo de digitar dados
